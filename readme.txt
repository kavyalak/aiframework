cd streamlit-doc-app
.\venv\Scripts\activate
streamlit run app.py
to create venv: python -m venv venv
source venv/bin/activate       # On macOS/Linux
venv\Scripts\activate          # On Windows
pip install -r requirements.txt
